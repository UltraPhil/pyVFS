class File(object):
    """
    Represents a file.  A file has a pointer to it's parent (directory), a filename and it's properties and attributes.
    """

    

    def __init__(self, *args):
        return super().__init__(*args)

